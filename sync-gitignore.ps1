# sync-gitignore.ps1
# 
# ⚠️ Цель этого скрипта — обход временного ограничения в инструменте AI-ассистента.
# В данный момент ассистент не может читать файл .gitignore напрямую из-за бага в системе.
# 
# 🔄 Решение:
# После каждого редактирования .gitignore через GitHub Desktop
# запускайте этот скрипт, чтобы создать копию в виде .gitignore.txt,
# которую ассистент сможет прочитать.
# 
# 💡 Как использовать:
# 1. Откройте терминал в корне проекта
# 2. Выполните: .\sync-gitignore.ps1
#    (или дважды щёлкните по файлу в проводнике)
# 
# ✅ После запуска:
# - Файл .gitignore будет скопирован в .gitignore.txt
# - AI-ассистент сможет увидеть последние изменения в правилах игнорирования
# 
# 🧼 Для чистоты истории — .gitignore.txt добавлен в .gitignore

$source = "D:/Apps/GitHub/KSP-Hub/My-CV-Engineering-Projects/.gitignore"
$backup = "D:/Apps/GitHub/KSP-Hub/My-CV-Engineering-Projects/.gitignore.txt"

if (Test-Path $source) {
    Copy-Item $source $backup
    Write-Host "✅ Синхронизация .gitignore -> .gitignore.txt успешна"
} else {
    Write-Host "❌ Файл .gitignore не найден. Убедитесь, что вы находитесь в корне репозитория."
}