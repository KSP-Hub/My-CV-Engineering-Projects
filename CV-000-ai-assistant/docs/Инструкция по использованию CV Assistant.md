# **НАЗНАЧЕНИЕ СКРИПТОВ:**

| Скрипт                       | Назначение                | Когда использовать                   |
| ---------------------------- | ------------------------- | ------------------------------------ |
| **`start_cv_assistant.ps1`** | **Полный запуск** системы | Первый запуск, после перезагрузки ПК |
| **`check_assistant.ps1`**    | **Проверка** состояния    | Проверка перед работой, диагностика  |
| **`reset_assistant.ps1`**    | **Аварийный сброс**       | Если система зависла или не работает |

🚀 **ПОРЯДОК РАБОТЫ:**
----------------------

### **Вариант 1: Нормальный запуск (каждый день)**

1. ✅ **Запустить `start_cv_assistant.ps1`**

2. ✅ **Дождаться сообщения "Gotovo!"** (≈15-20 секунд)

3. ✅ **Открыть VS Code и работать**

### **Вариант 2: Быстрая проверка**

1. ✅ **Запустить `check_assistant.ps1`**

2. ✅ **Посмотреть результаты:**
   
   * ✅ Зелёные галочки = система готова
   
   * ❌ Красные ошибки = запустить `reset_assistant.ps1`

### **Вариант 3: При проблемах**

1. ❌ **Запустить `reset_assistant.ps1`** (полный перезапуск)

2. ✅ **Запустить `check_assistant.ps1`** (проверить результат)

3. ✅ **Если всё зелёное → открыть VS Code**

⚠️ **ВАЖНЫЕ ЗАМЕЧАНИЯ:**
------------------------

1. **Запуск от администратора** — не требуется (если нет проблем с портами)

2. **Время ожидания** — скрипты ждут 5-10 секунд для запуска сервера

3. **GPU проверка** — если нет nvidia-smi, будет предупреждение (не ошибка)

4. **Модель** — используется `cv-assistant` (можно изменить в скриптах)

🔧 **ЕСЛИ НЕ РАБОТАЕТ:**
------------------------

1. **Проверить наличие Ollama** — `ollama --version` в PowerShell

2. **Проверить модель** — `ollama list` (должна быть cv-assistant)

3. **Запустить проверку** — `check_assistant.ps1` покажет что именно сломалось

4. **Полный сброс** — `reset_assistant.ps1` исправляет 90% проблем

📊 **ИНДИКАТОРЫ ГОТОВНОСТИ:**
-----------------------------

В `check_assistant.ps1` ищите:

* ✅ **"SISTEMA GOTOVA K RABOTE"** = можно работать

* ❌ **"OBNARUZhENI PROBLEMI"** = нужен перезапуск

* * *

**Коротко:**

1. **Запустил `start_cv_assistant.ps1` → работай**

2. **Сомневаешься → запусти `check_assistant.ps1`**

3. **Не работает → запусти `reset_assistant.ps1` → затем `check_assistant.ps1`**
