# Система условных обозначений завершения проекта

## Статусы проекта

### in_progress
- Проект находится в активной разработке
- Регулярные коммиты и обновления
- Активное взаимодействие с AI-ассистентом

### review
- Разработка завершена
- Проект находится на стадии проверки
- Проведение тестов и рефакторинг

### completed
- Все задачи завершены
- Проект готов к использованию
- Документация завершена

### archived
- Проект завершен и архивирован
- Не требует дальнейших изменений
- Может использоваться как пример

## Условные обозначения в файле статуса

В файле `project-status.json` используются следующие поля для обозначения статуса:

```json
{
  "status": "in_progress",        // Общий статус проекта
  "current_phase": "face_detection", // Текущая фаза разработки
  "completion_percentage": 75,    // Процент завершения (опционально)
  "last_review": "2025-12-04",   // Дата последнего обзора (опционально)
  "next_review": "2025-12-11"   // Дата следующего обзора (опционально)
}
```

## Теги Git для обозначения завершения

### Для завершения фазы
```
git tag -a "phase-completed-{phase_name}-{date}" -m "Завершение фазы {phase_name}"
```

Пример:
```
git tag -a "phase-completed-face_detection-20251204" -m "Завершение фазы детекции лиц"
```

### Для завершения проекта
```
git tag -a "project-completed-{project_name}-{date}" -m "Завершение проекта {project_name}"
```

Пример:
```
git tag -a "project-completed-cv_engineering-20251204" -m "Завершение проекта CV Engineering"
```

## Финальные отчеты

При завершении фазы или проекта создается финальный отчет:

```
docs/reports/completion-report-{type}-{date}.md
```

Где:
- `{type}` - тип отчета (phase, project)
- `{date}` - дата завершения (YYYY-MM-DD)

## Автоматическая проверка статуса

Скрипт `scripts/check_context.ps1` автоматически проверяет статус проекта и выводит соответствующее сообщение:

- ✅ Проект готов к работе
- ⚠️ Требуется обзор
- 🔄 В процессе разработки
- 📦 Проект завершен и архивирован

## Индикаторы завершения задач

В файле `project-status.json` задачи могут иметь следующие статусы:

- `completed` - задача завершена
- `in_progress` - задача в процессе выполнения
- `pending` - задача ожидает начала
- `review` - задача находится на проверке
- `blocked` - задача заблокирована

Пример:
```json
{
  "tasks": {
    "face_detection_implementation": "completed",
    "qr_code_detection": "in_progress",
    "testing_framework": "pending"
  }
}