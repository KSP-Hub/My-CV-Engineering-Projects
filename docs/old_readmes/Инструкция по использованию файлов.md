### Шаг 1: Создание папки для скриптов

```cmd
mkdir C:\Users\[Folder_name]\CV_Setup
```

### Шаг 2: Размещение файлов

Все три файла (`setup_all.cmd`, `start_ollama.cmd`, `start_project.cmd`) поместите в папку:

```cmd
C:\Users\[Folder_name]\CV_Setup
```

### Шаг 3: Запуск основной настройки

1. Правой кнопкой на `setup_all.cmd` → "Запуск от имени администратора"
2. Скрипт выполнит всю настройку и перезагрузит компьютер

### Шаг 4: Использование после перезагрузки

- Для запуска любого проекта: дважды кликните по `start_project.cmd`
- Ollama будет запускаться автоматически при входе в систему

⚠️ Важные примечания

- Все файлы должны быть именно в формате `.cmd` - это не текстовые документы!
- Правильная кодировка критична - если текст отображается криво, пересохраните с правильной кодировкой
- Запускайте от имени администратора - иначе могут быть проблемы с правами доступа

🔄 Как отключить автозапуск Ollama (когда не нужен)

```cmd
del "%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\start_ollama.cmd"
```

После этого вам не понадобится ручная настройка! 💪
